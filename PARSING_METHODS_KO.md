# 약물명·용량 파싱 방법 비교와 선택 근거

## 1. 파싱이란?

파싱(parsing)은 `Risp*erdal 2mg BID` 같은 원문에서 **표준 약물명**, **1회 용량**, **단위**, **복용 빈도**를 분리하고, 계산 가능한 `risperidone / 2 mg / 하루 2회 / 4 mg/day` 구조로 바꾸는 과정이다. 파싱 결과가 틀리면 뒤의 환산식이 정확해도 최종 결과는 틀린다.

현재 처리 순서는 다음과 같다.

`파일 읽기 → 열 찾기 → 한 셀의 여러 약물 분리 → 약물명 표준화 → 용량·빈도 추출 → 일일 용량 계산 → CMD/MED/ED95/DDD/CPZ_FGA 환산 → 검토 기록 저장`

## 2. 주요 방법 비교

| 방법 | 원리 | 장점 | 단점 | 이 프로젝트에서의 판단 |
|---|---|---|---|---|
| 정규표현식(Regex) | 숫자, mg, BID 같은 고정 패턴을 규칙으로 추출 | 빠르고 결과를 설명하기 쉬움 | 새로운 표기와 복잡한 문장에 약함 | **용량·단위·빈도 추출에 적합** |
| 사전 정확 매칭 | 제품명·성분명·한글명을 표준명 사전에 연결 | 매우 정확하고 감사 가능 | 사전에 없는 오탈자를 못 찾음 | **약물명 1차 판정에 적합** |
| 문자열 정규화 | 대소문자, 유니코드, 공백, 기호 차이를 제거 | `RISP*ERDAL`, `risper dal` 같은 변칙에 강함 | 너무 많이 지우면 서로 다른 단어가 같아질 수 있음 | **정확 매칭 전에 제한적으로 사용** |
| 유사도 매칭 | 편집거리 기반으로 가장 비슷한 사전 항목 탐색 | 오탈자 처리에 효과적이고 빠름 | 비슷한 다른 약물로 잘못 붙일 수 있음 | **높은 임계값의 2차 판정에 적합** |
| 규칙 기반 NER | 문장 내 약물·용량 개체를 규칙 조합으로 탐지 | 설명 가능하고 데이터가 적어도 동작 | 규칙 수가 늘면 유지보수가 어려움 | 현재 짧은 처방 문자열에는 충분 |
| 전통 ML(CRF 등) | 라벨된 토큰 특징으로 개체 경계를 학습 | 문맥 패턴을 학습 가능 | 라벨 데이터와 특징 설계가 필요 | 긴 자유서술 처방이 많아질 때 검토 |
| Transformer NER | BERT 계열 모델을 파인튜닝해 개체를 추출 | 다양한 문장과 오탈자 문맥에 강함 | 학습 데이터·GPU·버전 관리 필요, 근거 설명이 어려움 | 2,000명 이상의 라벨 데이터 확보 후 후보 |
| LLM 구조화 출력 | 모델에 원문을 주고 JSON 형태로 추출 | 개발이 빠르고 복잡한 문장을 잘 처리 | 비용·지연·재현성·개인정보·환각 위험 | 임상 데이터 자동 확정용 1차 엔진으로 부적합 |

## 3. 선택한 방법: 보수적 하이브리드 파서

현재 데이터는 짧은 약물 문자열과 표 형태가 중심이므로 **정규화 + 사전 정확 매칭 + 높은 임계값의 유사도 매칭 + Regex 용량 추출** 조합이 가장 적절하다.

1. `NFKC` 유니코드 정규화로 `㎎`와 `mg` 같은 표기를 통일한다.
2. 대소문자, 공백, 장식용 기호 차이를 정리한다.
3. `drug_alias.csv`에서 제품명·성분명·한글명·검증된 오탈자를 정확 매칭한다.
4. 정확 매칭이 실패할 때만 RapidFuzz 편집거리 매칭을 사용한다.
5. 점수가 임계값(현재 85) 미만이면 임의 환산하지 않고 오류/검토 대상으로 보낸다.
6. 용량·단위·빈도는 설명 가능한 Regex 규칙으로 추출한다.
7. PRN, 여러 용량, 상충 빈도, 주사제·주 단위 처방은 자동 계산하지 않는다.

이 방식은 작은 데이터에서도 즉시 동작하고, 각 결과가 왜 나왔는지 `match_type`, `match_score`, `warning`, `source_row`로 추적할 수 있다. `코티아핀`처럼 검증된 변칙은 사전에 추가하지만, `코틴`처럼 다른 단어일 가능성이 큰 짧은 문자열은 자동으로 quetiapine으로 확정하지 않는 것이 안전하다.

## 4. 코드에서 각 파일의 역할

| 파일 | 역할 |
|---|---|
| `services/file_reader.py` | CSV 인코딩과 Excel 시트·헤더를 읽음 |
| `services/column_detector.py` | 환자, 약물, 용량, 단위, 빈도 열을 이름과 내용으로 찾음 |
| `services/medication_splitter.py` | 한 셀의 여러 약물을 분리 |
| `services/parser.py` | 약물명 표준화, 용량·빈도 파싱, 검토 여부 결정 |
| `lookup/drug_alias.csv` | 표준 약물명과 허용 별칭·검증된 오탈자 사전 |
| `services/converter.py` | 일일 용량을 환산표 계수로 계산 |
| `services/exporter.py` | 계산 결과, 감사 기록, 오류, 방법 설명을 Excel로 저장 |
| `app.py` | 업로드부터 결과 다운로드까지 전체 흐름을 연결 |

## 5. Excel 입력 규칙

권장 형식은 `patient_id`, `drug`, `dose`, `unit`, `frequency` 다섯 열이다. 예시는 `P001 / Risperdal / 2 / mg / BID`이다. 한 열만 쓸 때는 열 이름을 `medication`으로 하고 값은 `Risperdal 2mg BID`처럼 쓴다.

- 지원 단위: `mcg`, `ug`, `μg`, `㎍`, `mg`, `㎎`, `g`
- 지원 빈도: `QD`, `BID`, `TID`, `QID`, `QHS`, `QAM`, `QOD`, 매일, 1일 N회
- 빈도가 없으면 QD로 가정하지만 반드시 `needs_review=True`로 표시한다.
- 한 셀에 여러 약물은 쉼표, 세미콜론, 줄바꿈, `+`로 분리할 수 있다.

## 6. 2,000명 데이터와 AUC 0.98 검증 계획

AUC는 단순 파싱 정확도와 같은 값이 아니다. 먼저 평가 목적을 정해야 한다. 예를 들어 “문자열이 특정 표준 약물인가”를 분류할 때 one-vs-rest ROC-AUC 또는 PR-AUC를 쓸 수 있다. 약물명 추출 자체는 **정확 일치율, precision, recall, F1**, 용량은 **정확 일치율과 MAE**, 전체 행은 **약물명+일일 용량 동시 정답률**이 더 직접적이다.

2,000명 데이터를 사용할 때는 환자 단위로 train/validation/test를 분리해야 한다. 같은 환자의 비슷한 처방이 양쪽에 들어가면 데이터 누수로 점수가 부풀려진다. 권장 절차는 다음과 같다.

1. 원문, 표준 약물명, 용량, 단위, 빈도, 정답/검토 여부를 사람이 이중 라벨링한다.
2. 환자 단위로 70/15/15 또는 5-fold 교차검증을 구성한다.
3. 대소문자, 공백, 기호, 한/영 전환, 삭제·삽입·치환 오탈자를 **훈련 세트에만** 증강한다.
4. 현재 하이브리드 파서를 baseline으로 기록한다.
5. 필요할 때 Transformer NER을 학습하고 동일한 고정 test set에서 비교한다.
6. 전체 점수뿐 아니라 약물별·오탈자 유형별·미인식률·위험한 오매칭률을 공개한다.
7. 목표 98%는 test set에서 확인된 지표와 95% 신뢰구간을 함께 보고한다.

오류를 무조건 자동 보정해 98%를 만드는 것보다, 불확실한 항목을 검토로 보내 **위험한 오매칭의 precision을 높이는 것**이 임상 관련 데이터에서는 더 중요하다. 현재 코드에는 학습 모델이나 2,000명 실제 데이터가 없으므로 “파인튜닝 완료” 또는 “AUC 0.98 달성”으로 표현하면 안 된다.

## 7. 다음 실험에서 기록할 항목

- 데이터셋 버전과 라벨 지침
- 환자 수, 행 수, 약물별 표본 수
- train/validation/test 환자 수
- 파서·모델 버전과 임계값
- exact/fuzzy/unresolved 비율
- 약물명 micro/macro F1, 용량 정확도, 전체 행 정확도
- ROC-AUC/PR-AUC의 정의와 계산 방식
- 오매칭 사례와 검토 전송 사례

이 기록이 있어야 GitHub를 보는 사람이 현재 단계, 선택한 알고리즘, 선택 이유, 실험 결과를 재현할 수 있다.
